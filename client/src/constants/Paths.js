const ROOT = "/";
const LOGIN = "/login";
const USERS = "/users";
const PROJECTS = "/projects/:id";
const BOARDS = "/boards/:id";
const CARDS = "/cards/:id";

export default {
  ROOT,
  LOGIN,
  PROJECTS,
  BOARDS,
  CARDS,
  USERS,
};
